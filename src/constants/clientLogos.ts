import type { ClientLogoEntry } from "@/lib/types";

// No client logos are seeded yet — the previous list lived in Sanity and
// wasn't exported before the dataset was decommissioned. Add real entries
// here ({ id, name, logoUrl, logoMimeType }) when available.
export const clientLogos: ClientLogoEntry[] = [];
