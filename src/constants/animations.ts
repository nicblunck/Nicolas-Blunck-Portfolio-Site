/**
 * Centralized animation timings (ms). Keep these in sync with the @keyframes
 * durations in globals.css when changing.
 */

export const HERO_REVEAL_DELAY_MS = 1300;

export const HERO_WORD_BOUNCE_LINE_1_START_MS = 200;
export const HERO_WORD_BOUNCE_LINE_2_START_MS = 760;
export const HERO_WORD_BOUNCE_STAGGER_MS = 160;

export const SCROLL_ARROW_DELAY_MS = 1500;

export const METRIC_COUNT_DURATION_MS = 900;
